language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gpress' ]; then cd .. && eval "mv $currentfolder generator-gpress" && cd generator-gpress; fi
